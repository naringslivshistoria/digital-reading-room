name: Continuous Integration

on:
  push:
    branches:
    - main

jobs:

  ci:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        context:
        - comprima-adapter
        - reading-room-frontend
        - reading-room-search

    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v3

      # TODO: Login to registry
      # - name: 🔐 Login to Docker Registry
      #   uses: docker/login-action@v2
      #   with:

      - name: 🔧 Build and push to Docker Registry
        uses: docker/build-push-action@v4
        with:
          context: ./packages/${{ matrix.context }}
          # TODO: Push
          # TODO: Build args for frontend image

      # TODO: Deploy to kubernetes
      # - name: 🔧 Setup kubectl
      #   uses: tale/kubectl-action@v1
      #   with:
      #     base64-kube-config: ${{ secrets.BASE64_KUBE_CONFIG }}

      # - name: 🐳 Redeploy
      #   run: |
      #     NOW=`date +'%s'`
      #     kubectl apply -f "k8s/deployment.yaml"
      #     kubectl -n testing patch deployment web --type=json -p='[{"op": "add", "path": "/spec/template/metadata/labels/date", "value": "'$NOW'"}]'
      #     kubectl -n testing rollout status -w "deployment/web"
